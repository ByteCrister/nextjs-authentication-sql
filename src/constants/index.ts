export const OTP_LENGTH = 6;
export const OTP_EXPIRY_MINUTES = 10;
export const OTP_MAX_ATTEMPTS = 5;

export const SESSION_DEFAULT_DAYS = Number(process.env.SESSION_DEFAULT_DAYS || 1);
export const SESSION_REMEMBER_DAYS = Number(process.env.SESSION_REMEMBER_DAYS || 30);
